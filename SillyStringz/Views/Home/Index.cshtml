@* @{
  Layout = "_Layout";
}

@using SillyStringz.Models;

<div class="form">
  <div class="engineers">
    <h3>@Html.ActionLink("Engineers!", "Index", "Engineers") | @Html.ActionLink("Add a new engineer!", "Create", "Engineers")</h3>
  </div>

  @if (@Model.Count == 0)
    {
      <h4>No engineers have been added yet!</h4>
    } 

      <ul>
      @foreach(Engineer engineer in ViewBag.Engineer)
      {
        <p>@Html.ActionLink($"{engineer.Name}", "Details", "Engineers", new { id = engineer.EngineerId })</p>
      }
    </ul>  


    <div class="machines">
    <h3>@Html.ActionLink("Machines!", "Index", "Machines") | @Html.ActionLink("Add a new Machine!", "Create", "Machines")</h3>

    @if (@Model.Count == 0)
    {
      <h4>No machines have been added yet!</h4>
    } 

    <ul>
      @foreach(Machine machine in ViewBag.Machine)
      {
        <p>@Html.ActionLink($"{machine.Name}", "Details", "Machines", new { id = machine.MachineId })</p>
      }
    </ul>  
  </div>

</div> *@
